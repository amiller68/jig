# Project Layout

Overview of the codebase structure. Update this as the project evolves.

## Directory Structure

```
<!-- Document your project structure. Example:

src/
├── components/     # React components
│   ├── ui/         # Generic UI components
│   └── features/   # Feature-specific components
├── hooks/          # Custom React hooks
├── lib/            # Utility functions
├── pages/          # Next.js pages (routes)
└── types/          # TypeScript type definitions

# or for a Rust project:

crates/
├── my-lib/         # Core library
├── my-cli/         # CLI binary
└── my-server/      # Server binary

src/
├── domain/         # Business logic
├── infra/          # Infrastructure (DB, HTTP)
└── api/            # API endpoints
-->
```

## Key Files

<!-- Important files and what they do. Examples:
- `src/index.ts` — Application entry point
- `src/config.ts` — Configuration loading
- `src/db/schema.sql` — Database schema
-->

## Entry Points

<!-- Where execution starts. Examples:
- **CLI**: `src/main.rs` / `src/cli.ts`
- **Server**: `src/server.ts` / `cmd/server/main.go`
- **Library**: `src/lib.rs` / `src/index.ts`
-->

## Configuration

<!-- Where configuration lives. Examples:
- `config/` — Environment-specific config
- `.env` — Environment variables (not committed)
- `package.json` / `Cargo.toml` — Project metadata
-->

## Tests

<!-- Where tests live. Examples:
- `tests/` — Integration tests
- `src/**/*.test.ts` — Unit tests alongside source
- `__tests__/` — Jest test directory
-->

## Build Output

<!-- What gets generated. Examples:
- `dist/` — Compiled output
- `target/` — Rust build artifacts
- `node_modules/` — Dependencies (not committed)
-->
